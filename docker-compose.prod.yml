version: "3.9"

services:
  app:
    build: .
    container_name: bookstore-app
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NODE_ENV: production
    restart: always